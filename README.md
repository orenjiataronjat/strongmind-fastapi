```console
pip3 install fastapi
pip3 install "uvicorn[standard]"
pip3 install sqlalchemy 
pip3 install pytest
pip3 install httpx

python3 -m uvicorn strongmind:app --reload

in order to run the pytest just run

python3 -m pytest
```